server:
  port: 0
  max-http-header-size: 10MB
  servlet:
    context-path: /sae-authenticator
logging:
  level:
    com: DEBUG
spring:
  application:
    name: sae-authenticator
  mvc:
    servlet:
      path: /api/v1
  profiles:
    active: dev